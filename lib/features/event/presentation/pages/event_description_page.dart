// Flutter imports:
import 'package:flutter/material.dart';

class EventDescriptionPage extends StatelessWidget {
  const EventDescriptionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Event Description Page'),
    );
  }
}
